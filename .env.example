
# Google Analytics 4 — measurement ID (starts with G-). Set only for
# Production in Vercel; leave unset or use a different ID for Preview
# so branch/PR traffic never lands in the real property.
NEXT_PUBLIC_GA_ID=
